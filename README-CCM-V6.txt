CONDOLENCE CARD MESSAGES - V6  (condolencecardmessages.com)
Polish pass on your feedback. 62 pages. Assets bumped to v=6.

IMPORTANT: your live nav looked broken because the site was serving OLD cached
CSS (pre-hamburger). Re-upload styles.css and hard-refresh; v=6 forces it fresh.
In the code the nav is correct: at desktop the hamburger is hidden and the CTA is
a normal pill; on mobile the hamburger shows and the wide CTA is hidden.

FIXED IN V6
- Card URL no longer touches the frame: "condolencecardmessages.com" now sits
  inside the frame's inner edge with clear space when any frame is selected.
- Professional action bar: one primary "Download this card", then a clean, evenly
  styled row - Share, Print, WhatsApp, Copy message, Save - instead of the mixed
  jumble of colours and sizes.
- LinkedIn size corrected from 1200x627 (a small landscape banner) to 1200x1200,
  the right size for a LinkedIn post image.
- Category pages: reduced the large empty gap under the title (tighter hero and
  section spacing) so the messages start sooner.
- Premium card palette: refreshed the 8 backgrounds to calmer, cohesive tones
  (Slate, Aubergine, Ivory, Eucalyptus, Dusty blue, Blush, Charcoal, Warm sand)
  that match the site's dignified slate brand.
- Mobile drawer is now a proper full-width panel with a top divider (no more
  cramped, run-together links).

CARRIED FROM V2-V5
- 50 keyword pages / 500 original messages, 6 hubs, A-Z index.
- Guided finder, wording checker, Quick Design presets, print sizes, better share.
- Free photo upload with Fit/Fill + zoom + drag (no cut-off heads).
- Full mobile/responsive; double opt-in email wired for authenticated SMTP.

QA: 0 broken links, 0 brand leaks, 0 horizontal overflow (360/390), nav correct
at desktop + mobile, action bar verified, LinkedIn 1200x1200, card-maker JS valid.

NOTE: after uploading, always hard-refresh (or the CDN/browser may keep old CSS).

STILL NEXT-PHASE: Pet / Rainbow Bridge vertical + designs; true folded
inside/outside print PDF with crop marks; more long-tail pages toward ~200.
