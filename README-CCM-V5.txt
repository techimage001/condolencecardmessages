CONDOLENCE CARD MESSAGES - V5  (condolencecardmessages.com)
The card maker is now feature-complete and premium. 62 pages.

NEW IN V5 (all client-side, all free, verified working)
1. QUICK DESIGN PRESETS - one tap sets background, font, frame, accent and text
   style together: Classic cream, Deepest sympathy, Quiet floral, Sage calm,
   Faith comfort, Pet remembrance, Golden memory, Modern minimal. Removes the
   "too many choices in a hard moment" problem.
2. GUIDED FINDER ("Find the words" tab) - answer 3 gentle questions (who you're
   writing to / who they lost / tone) and it suggests matching messages from the
   collection, with "Use this card" (loads straight into the maker) and "Copy".
   No AI API, no data leaves the device. This is the headline differentiator.
3. WORDING CHECKER ("Could this wording hurt?") - paste your own draft and it
   gently flags cliches that often land badly ("better place", "everything
   happens for a reason", "stay strong", "at least...", etc.) with kinder
   alternatives. Entirely in-browser.
4. REAL PRINT SIZES - added Card 5x7, A5 and US Letter at 300 DPI, plus a PRINT
   button that opens a clean print dialog. Download is full-resolution for
   printing at home with no watermark.
5. BETTER SHARING - WhatsApp button, Copy message, plus the existing native
   Share and Download.

CARRIED FROM V2-V4
- 50 keyword pages / 500 original messages, 6 hubs, A-Z index.
- Free photo upload with Fit/Fill, zoom and drag-to-reposition (no cut-off heads).
- Full mobile/responsive (hamburger nav, zero horizontal scroll).
- Calm premium palette; no mid-card brand stamp; all P0 copy leaks fixed.
- Double opt-in email capture wired for authenticated SMTP.

QA: 0 broken links, 0 brand leaks, 0 horizontal overflow (360/390px), all new
features verified in a headless browser, card-maker JS valid.

DEPLOY: assets bumped to v=5 (hard refresh after redeploy). Secrets outside
public_html; info@ mailbox; SMTP + SPF/DKIM/DMARC (you have now done this);
swap AdSense slot; upload; submit sitemap.

REMAINING (genuine next-phase, content/print side)
- Pet / Rainbow Bridge content vertical (new pages + code-drawn pet designs).
- True folded inside/outside PDF with crop marks for professional printing
  (the current print path covers home printing well).
- More long-tail pages toward ~200, published in staged batches.
